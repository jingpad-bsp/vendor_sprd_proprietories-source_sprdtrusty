type hal_tuistate_default, domain;
hal_server_domain(hal_tuistate_default, hal_tuistate);

type hal_tuistate_default_exec, exec_type, vendor_file_type, file_type;
init_daemon_domain(hal_tuistate_default)

allow hal_tuistate_default tuiwllistener:binder { call transfer };
